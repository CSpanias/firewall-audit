[project]
name = "firewall-audit"
version = "0.1.0"
description = "Lightweight firewall security assessment tool"
readme = "README.md"
requires-python = ">=3.10"

[project.scripts]
firewall-audit = "firewall_audit:main"
